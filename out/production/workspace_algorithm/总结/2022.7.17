周赛第三题：
     基数排序的解法，第一次写，可以多熟悉熟悉
     别的没啥好说

链表:

    lc_141,lc_142
     环形链表问题：
        1.检测一个链表中是否存在环形链使用快慢指针技巧，快指针移动两步，慢指针移动一步，
            如果存在环，那么链表必然存在环。
        2.此类问题往往会有衍生，如求解环入口结点，此时该问题就需要通过数学推导来解答，
        通过构建一个环形链表模型，观察快慢指针移动距离的关系，从而得到数学规律，再编码。
        3.解决此类环形问题也有通解，就是利用hashmap保存遍历的结点，这样就可以直接查表得到想要的答案。
    lc_160
    环形链表：
        1.官方解答将该题视为一个逻辑上的环形问题，很有意思的角度。
     快慢指针：
        1.两链表若相交，必然存在一段结点数量一致，我们求出两条链表的所有节点数，
            相减得到差值，令较长链表先移动差值个节点，再同时运动，就可以得到相交节点。

    lc_19
    栈：
        先进后出，刚好符合删除倒数第n个节点的特性。
    快慢指针：
        快先移动n个节点，再和慢一起移动，则快指向末尾节点时，慢敲好指向倒数第n个节点的前节点。
        快慢指针可以解决不少在链表上的数值问题。

    lc_161
    环形问题：
        将链表整成环形，再移动即可。
        环形链表可能不只是一种题型，也可能是一种解题的思路，可以适当扩展。
